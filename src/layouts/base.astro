---
// src/layouts/BaseLayout.astro
import Aside from "../components/aside.astro";
import Footer from "../components/footer.astro";
import Title from "../components/title.astro";

const { pageName } = Astro.props; //get the correct page name to set. in the title component
---

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>~ | Hitarth Thanki</title>
        <link rel="icon" type="image/x-icon" href="/logo.ico">
    </head>
    <body class="bg-Base flex flex-col justify-center items-center h-screen text-Text">
        <div class="flex w-[900px] h-[150px] bg-Base"> </div>
        <!-- Wrapper Container -->
        <div class="bg-base w-[900px] h-full flex flex-col">
            <!-- Hero Container -->
            <div class="flex flex-1 gap-[50px]">
                <!-- Aside Component -->
                <div class="flex flex-col items-end gap-[25px] bg-Base">
                    <Aside />
                </div>

                <!-- Main container -->
                <div class="flex flex-col items-center flex-1 bg-Base">
                    <Title page={pageName} />
                    <slot />
                </div>
            </div>

            <!-- Footer -->
            <Footer />
        </div>
    </body></html>
